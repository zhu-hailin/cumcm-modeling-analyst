# 逐题深度求解模板

**阶段状态：`STAGE_2_QUESTION_BY_QUESTION`**

> 当前问题先研究和讨论，用户确认后再正式实现。Codex 中的文件统一进入单赛题工作区，不把代码、数据和结果散落在根目录。

---

# 问题 {k}

## Codex 路径映射

当前问题默认使用：

```text
02_analysis/q{k}_solution.md          # 本问完整分析与教程
03_code/q{k}/                         # 本问正式代码
04_results/figures/q{k}/              # 本问图表
04_results/tables/q{k}/               # 本问结果表
04_results/data/q{k}/                 # 本问数值与跨问传递数据
04_results/logs/RUN_LEDGER.md         # 全局运行账本
04_results/logs/runs/Rxxx.md          # 重要运行详情
04_results/VISUALIZATION_MANIFEST.md  # 图表追溯
07_references/                         # 下载论文、网页与资料笔记
99_temp/                               # 一次性临时文件
```

用户已有合理结构或明确指定其他路径时，以用户要求为准。原题与官方附件留在 `00_problem/`，原始/清洗/外部数据分别进入 `01_data/raw/`、`processed/`、`external/`。

---

# A. 方案研究与讨论

这一部分完成后进入 `QUESTION_PLAN_CONFIRMATION`。确认前不要生成最终代码和完整成果。

## 本问要求回查

- 原题要求：
- 必须输出：
- 验收标准：
- 单位、范围和边界：
- 不能忽略的限制：
- 对应原题位置：

## 与前后问题的关系

- 上一问传入：
- 本问直接使用的共享数据/变量/参数：
- 本问需要输出给下一问：
- 当前方案可能影响前面哪些结论：
- 当前方案可能改变后面哪些模型：

## 数据审计与缺口

- 使用的官方原始数据：`00_problem/` / `01_data/raw/`
- 已有清洗数据：`01_data/processed/`
- 仍缺少的现实数据：
- 需要联网检索的数据：
- 外部数据拟保存到：`01_data/external/`
- 数据口径、时间范围、空间范围和单位：
- 无法可靠取得的数据与替代方案：

现实数据缺失时必须主动检索；找不到就报告缺口，不得编造观测值。

## 本问探索性研究

只做会影响方案选择的探索：

- 数据结构与规模：
- 缺失、异常和分布：
- 关键趋势、关系或约束：
- baseline / 小规模实验：
- 候选模型关键前提：
- 对 Stage 1 推荐是否有调整：无 / 有
- 若调整，原因：

确认前的脚本和图只属于探索，不得冒充最终代码、Final Run 或论文图。

## 候选方案比较

| 方案 | 核心方法 | 适配证据 | 优点 | 风险 | 推荐指数 | 切换条件 |
|---|---|---|---|---|---:|---|

不为了数量硬凑方案。

## 当前推荐方案

- 推荐方法：
- 一句话理由：
- 核心变量：
- 公式框架：
- 主要假设：
- 外部数据/参数需求：
- 验证方式：
- 与后续问题的接口：
- 当前最大风险：

这里只给足够讨论的公式框架，不提前写完整生产代码。

## 用户补充入口

自然询问用户是否还有：

- 论文或参考文献；
- 额外数据；
- 老师/队友建议；
- 自己更倾向的模型；
- 对假设、目标、约束或输出的修改意见。

用户补充后，先读取、核验并更新当前方案，再继续讨论。

## 方案确认

状态：

```text
QUESTION_PLAN_CONFIRMATION
```

只有用户明确表示“按这个做 / 执行 / 没有补充 / 就这样”等，才进入正式实现。

### 用户确认前禁止

- 在 `03_code/q{k}/` 写最终生产级代码；
- 建立 Final Run；
- 生成 A/B 级正式图；
- 生成最终结果表；
- 把中间结果写成最终答案；
- 完成 `02_analysis/q{k}_solution.md`；
- 跳到问题 {k+1}。

---

# B. 用户确认后的正式实现

## 最终模型定稿

### 变量与符号

本问符号同步到 `02_analysis/symbols.md`。

| 符号 | 含义 | 单位 | 代码对应 |
|---|---|---|---|
| `$x_i$` |  |  |  |

行内公式使用 `$...$`，独立公式使用 `$$...$$`。禁止裸写 LaTeX。

### 假设

采用、修改和废弃的假设同步到 `02_analysis/assumptions.md`。

### 模型与公式

每个实际用于求解的关键公式都要说明：

- 公式在做什么；
- 变量、参数与上下标；
- 单位；
- 为什么适合本题；
- 与前后公式的关系；
- 对应 `03_code/q{k}/` 中哪个文件和函数。

不得为了教程看起来高级而增加代码中没有使用的公式。

### 目标函数 / 核心关系

### 约束

按“公式 → 中文解释 → 代码位置”组织。

### 参数与来源

- 题目给定：
- 数据计算：
- 外部来源：
- 明确假设：
- 仍需人工确认：

## 正式代码

代码位置：

```text
03_code/q{k}/
```

- 入口文件：
- 关键模块：
- 公共函数：`03_code/common/`
- 输入数据：
- 输出目录：
- 关键参数：
- 运行命令：
- 与 `03_code/run_all.py` 的关系：

要求：

- 不硬编码本机私人绝对路径；
- 不覆盖 `01_data/raw/`；
- 清洗结果写入 `01_data/processed/`；
- 正式输出写入 `04_results/`；
- 临时调试文件写入 `99_temp/`。

## 图前证据契约

在写 A/B 级绘图代码前，执行 `figure-evidence-contract.md`。

```text
核心结论：
图的角色：结果 / 机制 / 验证 / 鲁棒性 / 决策
Hero evidence：
Supporting evidence：
panel a：
panel b：
panel c：
源数据：
预期 Run ID：
不确定性定义：
评阅风险：
```

每个 panel 必须有唯一作用。不得为了画面好看静默删数据、删类别、删失败种子或隐藏不利场景。

## 可视化与结果表计划

### A 级核心结果图

- 图名与目的：
- 输出：`04_results/figures/q{k}/`
- 是否进入论文：

### B 级诊断 / 验证图

- 图名与目的：
- 输出：`04_results/figures/q{k}/`

### C 级探索图

- 只保留真正影响判断的探索图：

### 核心结果表

- 表名：
- 输出：`04_results/tables/q{k}/`
- 是否进入论文：

### 数值与跨问结果

- 输出：`04_results/data/q{k}/`

没有某一类需求时写“无”，不得凑图凑表。

## 模型运行账本

凡会影响最终数字、验证、正式图表或论文结论的运行，写入：

```text
04_results/logs/RUN_LEDGER.md
```

重要运行另存：

```text
04_results/logs/runs/Rxxx.md
```

本问完成前必须明确：

```text
FINAL_RUN_ID = Rxxx
```

- Final Run ID：
- 关联 Validation Run：
- 代码入口：
- 输入数据版本：
- 输出目录：
- 是否替代旧 Run：

不得从聊天、旧截图、旧 CSV 或 `SUPERSEDED` Run 手抄最终数字。

## 实际运行结果

- Final Run ID：
- 关键数值：
- 最终参数：
- 核心结果表：
- 完整明细表：
- A 级图：
- B 级图：
- C 级图：
- 跨问传递文件：
- 运行限制：

只记录实际运行得到的结果。

## Visualization Manifest

更新：

```text
04_results/VISUALIZATION_MANIFEST.md
```

| 图号 | 文件 | 问题 | Run ID | 等级 | 数据源 | 生成代码 | 支撑结论 | 进入论文 |
|---|---|---|---|---|---|---|---|---|

## 验证与 Visual QA

按实际需要检查：

- 单位、边界和约束；
- baseline；
- 误差与残差；
- 敏感性与鲁棒性；
- 替代模型；
- 随机种子重复性；
- 启发式算法收敛；
- 每个 panel 的证据作用；
- 中文、单位、图例、字体、遮挡与裁切；
- 最终论文尺寸可读性；
- 图表、表格与 Final Run 一致。

失败状态必须修复或诚实记录，不能假装通过。

## 本问完整教程

正式完成后生成：

```text
02_analysis/q{k}_solution.md
```

至少包含：

- 题意和输入输出；
- 选模过程与用户确认结果；
- 数据来源和处理；
- 变量、假设、公式；
- 正式代码结构；
- 运行命令和 Final Run；
- 图表、表格和结果解释；
- 验证；
- 常见错误；
- 论文写作时应如何表达；
- 向下一问传递什么。

## 资料与文献

本问实际使用的资料保存和记录到：

```text
07_references/papers/
07_references/websites.md
07_references/notes.md
```

| 资料 | 类型 | 核验状态 | 本问用途 | 是否进入论文 |
|---|---|---|---|---|

不为丰富列表堆无关文献。

## 本问最终答案与交接

- 直接回答原题：
- Final Run ID：
- 传递给下一问的数据：
- 传递文件：
- 传递参数/变量：
- 下一问需要注意：
- 尚未解决的限制：

同时更新根目录 `README.md` 和 `02_analysis/model_plan.md`。

---

# C. 本问完成检查

- [ ] 用户已确认本问方案；
- [ ] 原题要求已直接回答；
- [ ] 原题/附件未被覆盖；
- [ ] raw、processed、external 数据分层正确；
- [ ] 正式代码位于 `03_code/q{k}/`；
- [ ] 输出集中在 `04_results/`；
- [ ] `FINAL_RUN_ID` 已写入 Run Ledger；
- [ ] 图表和表格来自 Final/Validation Run；
- [ ] `VISUALIZATION_MANIFEST.md` 已更新；
- [ ] 正式图逐 panel 通过 QA；
- [ ] 关键公式格式和说明正确；
- [ ] `02_analysis/q{k}_solution.md` 已完成；
- [ ] 文献和网页来源已记录；
- [ ] 下一问接口明确；
- [ ] 根目录 `README.md` 已更新；
- [ ] 根目录和 `99_temp/` 没有散落唯一正式成果。

通过后，进入问题 {k+1} 的方案研究与讨论。

---

# D. 聊天窗口摘要

方案讨论阶段，聊天只说推荐方案、依据、风险和希望用户补充什么。

正式完成阶段，默认用 2–6 句说明：我最终做了什么、最重要结果、为什么可信、对下一问有什么用、文件保存在哪里。

情绪反馈必须来自真实结果，不得无依据夸张。
