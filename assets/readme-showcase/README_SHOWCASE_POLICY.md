# README 展示策略

README 的目标是介绍项目能力，不应成为旧题答案库。

当前首页只使用不含赛题数据的矢量宣传图。若未来公开旧题案例图，应单独放入 `docs/cases/`，并同时标注题目、日期、Skill/模型版本、联网边界、盲测冻结状态、Final/Validation Run 与 `POST_HOC` 边界。

任何公开案例都不能被自动加载为当前赛题资料，也不能用于证明对未知题目的普遍性能。
